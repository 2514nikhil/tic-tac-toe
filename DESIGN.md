# DESIGN.md

> **Source:** Stitch Project `4495987415004391050`
> **Fetched:** 2026-04-24
> **Status:** Project has no design system or screens configured yet.

---

## Project Info

| Property     | Value                     |
|--------------|---------------------------|
| Project ID   | `4495987415004391050`     |
| Visibility   | Private                   |
| Type         | Text-to-UI Pro            |
| Origin       | Stitch                    |
| Created      | 2026-03-21                |
| Role         | Owner                     |

---

## Color Palette

> ⚠️ No design system found in this Stitch project. Populate the palette below manually or apply a design system via Stitch.

| Token                  | Hex Value   | Usage                          |
|------------------------|-------------|--------------------------------|
| `--color-primary`      | `#——————`   | Primary actions, CTAs          |
| `--color-primary-dark` | `#——————`   | Hover / active states          |
| `--color-secondary`    | `#——————`   | Accents, highlights            |
| `--color-background`   | `#——————`   | Page background                |
| `--color-surface`      | `#——————`   | Cards, panels                  |
| `--color-on-surface`   | `#——————`   | Text on surface                |
| `--color-on-primary`   | `#——————`   | Text/icons on primary color    |
| `--color-error`        | `#——————`   | Error states                   |
| `--color-success`      | `#——————`   | Success states                 |
| `--color-border`       | `#——————`   | Borders, dividers              |

---

## Typography

> ⚠️ No typography tokens found. Fill in once a design system is applied.

| Token                    | Value       | Usage                          |
|--------------------------|-------------|--------------------------------|
| `--font-family-base`     | `——————`    | Body text                      |
| `--font-family-heading`  | `——————`    | Headings (h1–h4)               |
| `--font-family-mono`     | `——————`    | Code, monospaced text          |
| `--font-size-xs`         | `——`        | Captions, labels               |
| `--font-size-sm`         | `——`        | Secondary text                 |
| `--font-size-md`         | `——`        | Body / default                 |
| `--font-size-lg`         | `——`        | Subheadings                    |
| `--font-size-xl`         | `——`        | Section headings               |
| `--font-size-2xl`        | `——`        | Page titles                    |
| `--font-weight-regular`  | `400`       | Normal text                    |
| `--font-weight-medium`   | `500`       | Emphasis                       |
| `--font-weight-bold`     | `700`       | Headings, strong emphasis      |
| `--line-height-tight`    | `1.2`       | Headings                       |
| `--line-height-normal`   | `1.5`       | Body text                      |

---

## Shape & Spacing

| Token                 | Value   | Usage                          |
|-----------------------|---------|--------------------------------|
| `--radius-sm`         | `4px`   | Inputs, small components       |
| `--radius-md`         | `8px`   | Cards, buttons                 |
| `--radius-lg`         | `16px`  | Modals, panels                 |
| `--radius-full`       | `9999px`| Pills, badges                  |
| `--spacing-unit`      | `8px`   | Base spacing unit              |

---

## Appearance

| Property    | Value      |
|-------------|------------|
| Default Mode | Light     |
| Dark Mode   | Supported |

---

## Design Notes

_Add any free-form design guidelines, brand rules, or UI principles here._

- [ ] Apply a Stitch design system to auto-populate colors and fonts
- [ ] Add screens to the Stitch project to generate component-level tokens
- [ ] Define brand voice and iconography guidelines

---

*This file is auto-generated from Stitch MCP data. Re-run the fetch to refresh when a design system is applied.*
